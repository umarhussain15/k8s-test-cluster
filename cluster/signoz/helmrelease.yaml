apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: signoz
spec:
  chart:
    spec:
      chart: signoz
      version: 0.79.x
      sourceRef:
        kind: HelmRepository
        name: signoz
  interval: 10m0s
  install:
    crds: CreateReplace
  upgrade:
    crds: CreateReplace
